// if elseif else example
let bool = false, a = true, x = 0;
let result = bool? {
    `x = x + 1`;
    "bool is " + bool + "!"!
} : (a + bool)? {
    x = x - 1;
    "a plus bool is " + (a + bool)!
} : {
    x = x + 2;
    `"nothing worked!"`;
};

`result`;
`x`;
